name: Validate PR Documentation

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  validate-pr-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug: show event payload (for troubleshooting)
        run: |
          echo "---- GITHUB EVENT PATH ----"
          echo "$GITHUB_EVENT_PATH"
          echo "---- EVENT JSON (first 200 lines) ----"
          head -n 200 "$GITHUB_EVENT_PATH" || true

      - name: Validate PR Documentation Checkboxes & Links
        shell: bash
        run: |
          set -euo pipefail
          echo "🔍 Extracting PR body..."
          BODY=$(jq -r '.pull_request.body // ""' "$GITHUB_EVENT_PATH")
          echo "---- PR BODY START ----"
          echo "$BODY"
          echo "---- PR BODY END ----"

          # Normalize body to lower-case for keyword checks where needed
          BODY_LOWER=$(printf "%s" "$BODY" | tr '[:upper:]' '[:lower:]')

          # Check for checked boxes: we accept variants like "- [x] frontend" or "- [x] **Frontend**"
          FRONTEND_CHECKED=false
          BACKEND_CHECKED=false
          BOTH_CHECKED=false

          if printf "%s" "$BODY" | grep -qiE '\- \[\s*x\s*\]\s*\*{0,2}\s*frontend'; then
            FRONTEND_CHECKED=true
            echo "✅ Frontend checkbox checked"
          fi

          if printf "%s" "$BODY
